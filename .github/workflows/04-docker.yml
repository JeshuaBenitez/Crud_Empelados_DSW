name: 04 Docker Hub

on:
  repository_dispatch:
    types:
      - chain-docker

jobs:
  docker:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest
    if: ${{ github.event.client_payload.branch == 'deploy' }}
    permissions:
      contents: read

    env:
      IMAGE_TAG: ${{ github.event.client_payload.sha }}
      DOCKER_USER: ${{ secrets.DOCKER_USERNAME }}

    steps:
      - name: Checkout merge commit
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.client_payload.sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./DSW02-Practica01
          file: ./DSW02-Practica01/Dockerfile
          push: true
          tags: ${{ env.DOCKER_USER }}/crud-empleados-api:${{ env.IMAGE_TAG }}

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend-angular
          file: ./frontend-angular/Dockerfile
          push: true
          tags: ${{ env.DOCKER_USER }}/crud-empleados-frontend:${{ env.IMAGE_TAG }}
